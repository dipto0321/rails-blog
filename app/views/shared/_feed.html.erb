<% if @feed_items.any? %>
   <ol class="list-group">
     <%= render @feed_items  %>
   </ol>
  <%= will_paginate @feed_items , previous_label: '&laquo; PREV', next_label: 'NEXT &raquo;', list_classes: %w(pagination justify-content-center mt-3) %>
<% end %>